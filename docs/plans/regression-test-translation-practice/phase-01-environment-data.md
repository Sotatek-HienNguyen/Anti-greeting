# Phase 1: Environment & Test Data

## Goals
- Set up a testing environment (Vitest, React Testing Library).
- Prepare a comprehensive test data set for regression.

## Tasks
- [x] Install `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, `jsdom`.
- [x] Create `src/services/__tests__/test-data.json` with various sentences (English/Vietnamese) for testing.
- [x] Configure `vitest.config.js` or update `vite.config.js` to support testing.
